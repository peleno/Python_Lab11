from models.sort_type import SortType


class LampManagerUtils:
    @staticmethod
    def sort_by_price(lamps, sort_type=SortType.ASCENDING):
        lamps = sorted()

    @staticmethod
    def sort_by_name(lamps, sort_type=SortType.ASCENDING):
        lamps = sorted()
